replicasCount: 1

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70

image:
  url: cybergoatz.azurecr.io/training-service
  tag: v1.2.3

resources:
  requests:
    cpu: 300m
    memory: 768Mi
  limits:
    cpu: "2"
    memory: 1536Mi

environments:
  SERVICE_PRECONDITION: uag-service:8084
secretEnvironments:
  TRAINING_SANDBOX_SERVICE_ACCOUNT_CLIENT_SECRET:
    secretName: training-service-service-account
    secretKey: client-secret

mounts:
  training:
    extension: .properties
    mountPath: /app/etc/
    isOneFile: true
    subPath: false
    type:
      configMap:
        name: training-service-configmap
  tls:
    extension: .crt
    mountPath: /srv/
    isOneFile: true
    subPath: false
    type:
      secret:
        secretName: crczp-certs

connectionTimeout:
maximumPoolSize:
databasePool:
  maximumPoolSize: 8
  minimumIdle: 1
  connectionTimeout: 10000
  idleTimeout: 600000
  maxLifetime: 1800000
  leakDetectionThreshold: 0

sandboxExpiration:
  enabled: true
  scanDelayMs: 300000
  initialDelayMs: 60000

sandboxServiceAccount:
  tokenUri: http://keycloak-service:8080/keycloak/realms/cybergoatz/protocol/openid-connect/token
  clientId: training-service
  clientSecret: training-service-secret
