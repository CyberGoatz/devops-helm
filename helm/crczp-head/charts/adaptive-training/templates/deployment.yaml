{{- $ports := list (dict "port" .Values.global.services.adaptiveTraining.port "protocol" "TCP" "probe" true) -}}
{{- include "common.deployment.header" (dict "name" .Values.global.services.adaptiveTraining.name "replicasCount" .Values.replicasCount "autoscaling" .Values.autoscaling "chart" .Chart "release" .Release "extraLabels" .Values.extraLabels) }}
{{- include "common.deployment.containers" (dict "name" .Values.global.services.adaptiveTraining.name "ports" $ports "Values" .Values) }}
          lifecycle:
            postStart:
              exec:
                command:
                  - "/bin/bash"
                  - "-c"
                  - "/opt/java/openjdk/bin/keytool -importcert -alias crczp -file /srv/tls.crt -cacerts -storepass changeit -noprompt"
      {{- if .Values.global.imageCredentials.username }}
      imagePullSecrets:
        - name: github-auth-secret
      {{- end }}
{{- include "common.deployment.volumes" (dict "Values" .Values) }}
{{- include "common.hpa" (dict "name" .Values.global.services.adaptiveTraining.name "Values" .Values "chart" .Chart "release" .Release "extraLabels" .Values.extraLabels) }}
